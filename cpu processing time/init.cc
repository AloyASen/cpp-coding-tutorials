#include <dos.h>
#include <time.h>
void main(){
    clock_t stop,start;
    start_clock();//calling clock to start counting tick
    for (i=0;i<=n;i++){
        s+=a[i] *p;
        p= p*x;

    }
    stop_clock();
    printf("no of ticks %d", (<Start time -stop time>))
    printf("no of seconds= %f", (start time -stop time)/CLK_TCK);
}//main ends